package spike.contractTest

fun main(args: Array<String>) { 
    println(
        """
            ============================================================

            Run "./gradlew test" instead.
            Then checkout /build/pacts for the new consumer contract.

            ============================================================
        """.trimIndent()
    )
}
